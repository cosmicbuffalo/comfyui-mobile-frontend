# Copy to .env.local and fill in your values. .env.local is gitignored.
# Baked into the production bundle at build time.

VITE_FEEDBACK_ENDPOINT=https://feedback.comfyui-mobile-frontend.com/feedback
